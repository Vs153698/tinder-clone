import { View, Text } from 'react-native';
import React from 'react';

const Header = () => {
    console.log(1=2);
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export default Header;
